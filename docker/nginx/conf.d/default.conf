server {
    # バーチャルサーバが使用するアドレス、ポートを指定
    listen 80;
    # バーチャルサーバで使用するホスト名を指定
    server_name jp.php-dev2.jp;
    # $document_root の値になる
    root /var/www/dev-web/;
    # インデックスページを指定
    index index.php;

    charset utf-8;

    location ~ / {
        alias /var/www/dev-web/public/;
        index index.php index.html index.htm;
        location ~* \.php$ {
            fastcgi_split_path_info  ^(.+¥.php)(/.+)$;
            # fastcgi_pass   127.0.0.1:9000;
            # fastcgi_pass   /usr/local/sbin/php-fpm;
            fastcgi_pass   php:9000;
            fastcgi_index  index.php;
            include        fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
        }
    }

    # location ~ [^/]\.php(/|$) {
    #     # PATH_INFO 部の分割に使用する正規表現を指定
    #     # 一つ目 ( .+\.php ) は $fastcgi_script_name の値になり、二つ目 ( /.+ ) は $fastcgi_path_info の値になる
    #     fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #     if (!-f $document_root$fastcgi_script_name) {
    #         return 404;
    #     }
    #     # FastCGI サーバへリクエストをプロキシする
    #     # fastcgi_pass 127.0.0.1:9000;
    #     fastcgi_pass unix:/var/run/php-fpm.sock;
    #     # スラッシュで終わる URI の後に追加されるファイル名を設定
    #     # $fastcgi_script_name の値になる
    #     fastcgi_index index.php;
    #     # 設定ファイルを読み込む
    #     include fastcgi_params;
    #     # FastCGI サーバに渡されるべきパラメータを設定
    #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #     fastcgi_param PATH_INFO $fastcgi_path_info;
    #     fastcgi_param PATH_TRANSLATED $document_root$fastcgi_path_info;
    # }

    # location ~ [^/]\.php(/|$) {
    #     fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #     if (!-f $document_root$fastcgi_script_name) {
    #         return 404;
    #     }
    #     fastcgi_pass php:9000;
    #     fastcgi_index index.php;
    #     include fastcgi_params;
    #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #     fastcgi_param PATH_INFO $fastcgi_path_info;
    # }
}